#read in the data from the high.score file
#identify which of those users had the highest scores automatically
#output the name and score of the winner

print("current leader")

while True:
  f = open("high.score", "r")
  contents = f.readline().strip()
  if 
  
